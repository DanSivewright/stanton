import React from 'react'

import './platform.css'

export const metadata = {
  title: 'PIMMS Platform — Unified Demo',
  description: 'UI demonstration of the unified PIMMS enterprise platform',
}

export default function PlatformLayout({ children }: { children: React.ReactNode }) {
  return children
}
